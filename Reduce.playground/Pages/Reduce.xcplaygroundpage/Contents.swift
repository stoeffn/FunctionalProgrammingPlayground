/*:
 [Previous](@previous)

 Puh! A nearly avoided disaster.
 
 Now all we need to do is combine the ingredients and we are done. Easy, right?
 
 * example
 This is how to do it in an imperative—the usual—way.
 ````
 var salad = plate
 for item in ingredients {
    salad = salad.combine(with: item)
 }
 ````
 
 But as you might have guessed, there is a functional alternative! There is a method called `reduce` that takes an initial result and a closure, which
 */

//let plate: Ingredient = "🍽"

//ingredients.reduce(plate) { $0.combine(with: $1) }

//: [Next](@next)
